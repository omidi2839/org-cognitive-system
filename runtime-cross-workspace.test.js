import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {MemoryRepository} from '../src/infrastructure/memoryRepository.js';import {MockAIGateway} from '../src/ai/mockGateway.js';import {CognitiveService} from '../src/application/service.js';
const actor={organizationId:'ORG:CROSS',personId:'PER:1',roles:['expert'],correlationId:'CROSS'};const setup=()=>new CognitiveService(new MemoryRepository(),new MockAIGateway());
test('direct-open assets are relative and one-click launcher exists',()=>{const h=fs.readFileSync('index.html','utf8');assert.match(h,/href="\.\/styles\.css"/);assert.match(h,/src="\.\/app\.js"/);assert.ok(fs.existsSync('START_BUILD.bat'));});
test('cross-object workspace returns structured sections without fabricated data',async()=>{const s=setup();const r=await s.executeCommand(actor,{sessionId:'C1',text:'وضعیت پروژه تحول دیجیتال را بررسی کن'});assert.equal(r.intent,'workspace.cross_object');assert.equal(r.workspace.componentType,'cross_object_workspace');assert.ok(Array.isArray(r.workspace.sections));assert.ok(r.workspace.sections.some(x=>x.key==='documents'));assert.ok(r.workspace.sections.some(x=>x.key==='meetings'));});
test('frontend has cross-object renderer',()=>{const j=fs.readFileSync('public/app.js','utf8'),c=fs.readFileSync('public/styles.css','utf8');assert.match(j,/cross_object_workspace/);assert.match(c,/\.cross-workspace/);});
