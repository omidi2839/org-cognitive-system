# 0.9.9.0.29 — گردش‌کار نقش‌محور میز پژوهش

- خبره فقط «ثبت و ارسال نظر» دارد و دکمه مرحله بعد نمی‌بیند.
- ثبت نظر خبره، پرونده را برای لایه «تکمیل و نقد» آماده می‌کند.
- خبره ارشد فقط «ثبت و ارسال نقد» دارد و دکمه مرحله بعد نمی‌بیند.
- ثبت نقد خبره ارشد، پرونده را برای لایه «جمع‌بندی» آماده می‌کند.
- فقط در مرحله جمع‌بندی، دکمه «تأیید نهایی مفهوم‌سازی و پایان میز پژوهش» نمایش داده می‌شود.
- نهایی‌سازی صریح باقی می‌ماند و مفاهیم قابل اتکا را برای دانش کلان تثبیت می‌کند.
- نقش ثبت‌کننده در پاسخ و تاریخچه ذخیره می‌شود.
- آکاردئون مفاهیم نسخه 0.9.9.0.28 و Dialog داخلی سینا بدون تغییر حفظ شده‌اند.
