import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {MemoryRepository} from '../src/infrastructure/memoryRepository.js';import {MockAIGateway} from '../src/ai/mockGateway.js';import {CognitiveService} from '../src/application/service.js';const actor={organizationId:'ORG:MA',personId:'PER:MA',roles:['manager']};test('agenda requires attention',async()=>{const s=new CognitiveService(new MemoryRepository(),new MockAIGateway());assert.equal((await s.buildManagementAgenda(actor,{})).status,'blocked')});test('agenda is proposed without authority',async()=>{const s=new CognitiveService(new MemoryRepository(),new MockAIGateway());const q=[{ref:'D1',title:'تصمیم',rank:1,score:95,focusWindow:'now',type:'decision'},{ref:'C1',title:'بازبینی',rank:2,score:80,focusWindow:'next',type:'cognition_review'},{ref:'W',title:'پایش',rank:3,score:30,focusWindow:'watch',type:'warning'}];const r=await s.buildManagementAgenda(actor,{attention:{queue:q},maxAgendaItems:2});assert.equal(r.agenda.agendaItems.length,2);assert.equal(r.agenda.deferred.length,1);assert.equal(r.agenda.canonical,false);assert.equal(r.governance.decisionNotAutomaticallyMade,true)});test('renderer and api exist',()=>{assert.match(fs.readFileSync('public/app.js','utf8'),/management_agenda/);assert.match(fs.readFileSync('api/index.js','utf8'),/management-agenda/)})