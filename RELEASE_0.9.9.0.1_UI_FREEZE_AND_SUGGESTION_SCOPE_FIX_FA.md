# Hotfix 0.9.9.0.1

این Hotfix دو Regression نسخه 0.9.9.0 را اصلاح می‌کند:

1. **هنگ کردن صفحه هنگام «افزودن سند»**
   - علت: حلقه بازگشتی `MutationObserver` در لایه قرارداد ارتباط حقوقی بود.
   - `sync()` خودش DOM را تغییر می‌داد و Observer دوباره همان `sync()` را صدا می‌زد.
   - Observer سراسری دیگر روی فرم Configureشده دوباره `sync()` اجرا نمی‌کند.
   - فقط اضافه/حذف مستقیم ردیف‌های ماده/تبصره در `itemsBody` باعث Sync می‌شود.

2. **پیشنهادهای جستجوی بانک اسناد**
   - پنل ثابت پایین صفحه کاملاً حذف شد.
   - پیشنهادها فقط هنگام تایپ داخل کادر فرمان در «میزکار شناختی من» ظاهر می‌شوند.
   - با خروج از میزکار شناختی، Dropdown نمایش داده نمی‌شود.
   - پنل‌های باقی‌مانده از Build قبلی نیز به‌صورت دفاعی از DOM حذف می‌شوند.

تمام قابلیت‌های 0.9.9.0 از جمله قرارداد صریح اصلاحیه/الحاقیه/ملغی/استفسار و موتور جستجوی بانک اسناد حفظ شده‌اند.

تعداد فایل‌های top-level پوشه `api`: 8
`api/auth-proxy.js`: وجود ندارد
