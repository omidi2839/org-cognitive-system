# Build 0.8.4 — Open Semantic Concept Extraction V1

تغییر معماری:
- وابستگی اصلی به `compoundEntities` و `semanticTerms` حذف شد.
- استخراج Candidate Concept از خود ساختار هر گزاره انجام می‌شود.
- عبارت‌های 1 تا 4 واژه‌ای، صفات، گروه هدف، نیاز، ذی‌نفع/دامنه، موجودیت و منبع معرفتی نامزد می‌شوند.
- مفهوم مرکب و اجزای آن هر دو می‌توانند Candidate باشند تا Human Validation مرز نهایی را تعیین کند.
- هر Concept اکنون type / score / evidence / role / source دارد.
- پرسش اول هر گزاره اعتبارسنجی «کامل بودن مفاهیم استخراج‌شده» است.
- سؤال جداگانه‌ای برای تعیین مرز مفهوم مستقل/مرکب/صفت/نیاز/ذی‌نفع ایجاد می‌شود.
- هیچ سقف ثابت برای تعداد جمله، Concept، Relation یا Question اعمال نشده است.

محدودیت صادقانه:
این نسخه Open Semantic Extractor قاعده‌محور و عمومی‌تر است، اما هنوز LLM/NER واقعی نیست. هدف Build 0.8.4 سنجش این است که آیا Recall مفاهیم در اسناد واقعی به شکل محسوسی بهتر می‌شود؛ اگر نه، مرحله بعد Provider-based Semantic Analyzer خواهد بود.
