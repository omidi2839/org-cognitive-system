# Build 0.7.6.4 — Document Separation & Analysis Queue

اصلاح دو خطای مفهومی/عملکردی:
1) اسناد بالادستی و عمومی اکنون با `documentClass` در Backend ذخیره و در مخزن‌ها جداگانه شمارش و نمایش داده می‌شوند.
2) «تحلیل اسناد» دیگر فرم آپلود نشان نمی‌دهد؛ فهرست اسناد ثبت‌شده از هر دو مخزن را نمایش می‌دهد و کاربر سند موردنظر را برای تحلیل انتخاب می‌کند.

نکته: اسناد قدیمی که قبل از ذخیره‌شدن `documentClass` ایجاد شده‌اند، به‌صورت `unclassified` در نظر گرفته می‌شوند و وارد هیچ‌یک از دو مخزن نمی‌شوند تا طبقه‌بندی اشتباه ایجاد نشود.

ZIP از نوع Full Replacement است.
