# 0.9.9.0.33 — رفع قطعی کلیک ثبت سند

علت:
در 0.9.9.0.32 برای بازگرداندن ظاهر کارت‌ها، کلاس `capability-card` دوباره اضافه شد.
MutationObserver همان کارت استاتیک را دوباره «کارت قدیمی» تشخیص می‌داد و clone می‌کرد؛ در نتیجه listener دکمه ثبت از بین می‌رفت.

اصلاح:
- کارت استاتیک فقط یک‌بار clone می‌شود.
- first pass صراحتاً `.k9931-static-document-card` را نادیده می‌گیرد.
- listener دکمه ثبت فقط یک‌بار bind می‌شود و با `data-k9933-bound` پایدار می‌ماند.
- ظاهر 0.9.9.0.32 و غیرفعال‌بودن Navigation خود کارت‌ها حفظ شده است.
