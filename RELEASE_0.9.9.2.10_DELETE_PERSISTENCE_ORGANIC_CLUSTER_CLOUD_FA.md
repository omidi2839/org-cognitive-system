# SINA 0.9.9.2.10

## حذف سند
- پس از DELETE، مخزن دوباره خوانده می‌شود و فقط وقتی وضعیت `deleted` واقعاً پایدار شده باشد پاسخ موفق داده می‌شود.
- بانک اسناد در سطح API نیز اسناد `deleted` را صریحاً حذف می‌کند؛ حتی اگر لایه قدیمی‌تر آنها را برگرداند.
- پس از حذف موفق، ردیف از UI حذف و سپس بانک اسناد Refresh می‌شود.
- asset بانک اسناد با نام cache-safe جدید `document-bank-099210.js` فعال شده است.

## ابر کلمات
- ساختار منظم grid حذف شد.
- هر موضوع کلان یک مرکز/لنگر مستقل دارد.
- زیرموضوع‌ها به‌صورت نامنظم در حلقه‌های اطراف همان موضوع کلان قرار می‌گیرند.
- جای موضوعات کلان در کل صفحه نیز با مارپیچ golden-angle و نامنظم تعیین می‌شود.
- الگوریتم collision از روی‌هم‌افتادن واژه‌ها جلوگیری می‌کند.
- فونت‌ها کوچک‌تر شدند:
  - موضوع کلان: حدود 14.5 تا 22px
  - زیرموضوع: حدود 8.5 تا 12.5px
- نسبت اندازه همچنان بر اساس تعداد اسناد حفظ شده است.
- assetهای ابر کلمات با نام cache-safe جدید `sina-topic-tree-099210.js/css` فعال شده‌اند.
