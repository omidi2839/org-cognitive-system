# Hotfix 0.9.1.6 — Document Bank Focus Mode + Search Occurrence Visibility

این بسته دو اصلاح را هم‌زمان انجام می‌دهد:

1. بانک اسناد به فضای کاری مستقل تبدیل می‌شود.
   - با ورود به بانک اسناد، `workspaceContext` (بخش «دانش و اسناد سازمان») موقتاً مخفی می‌شود.
   - Header بانک اسناد sticky است.
   - دکمه «بازگشت به دانش و اسناد سازمان» در بالای صفحه اضافه می‌شود.
   - با بازگشت، بانک بسته و Workspace اصلی دوباره نمایش داده می‌شود.

2. ابهام «کم بودن نتایج جستجو» رفع می‌شود.
   - معماری قبلی یک نتیجه به ازای هر سند برمی‌گرداند، نه یک نتیجه به ازای هر occurrence.
   - Backend جدید تعداد واقعی occurrence های عبارت را در متن هر سند محاسبه می‌کند.
   - `matchCount` و `matchSnippets` برگردانده می‌شود.
   - تا 12 محل تطابق برای هر سند در پاسخ می‌آید.
   - UI سه تطابق اول را مستقیم و بقیه را داخل بخش بازشونده نشان می‌دهد.
   - تعداد کل تطابق‌های متنی بالای صفحه نمایش داده می‌شود.
   - اسناد دارای تطابق بیشتر هنگام جستجو بالاتر قرار می‌گیرند.

## فایل‌ها

- `api/document-bank-core.js`
- `public/document-bank-091.js`
- `public/document-bank-091.css`

## Deploy

سه فایل فوق را جایگزین فایل‌های متناظر در Repository کنید و Deploy جدید Vercel را اجرا کنید.

## تست پذیرش

- ورود به بانک اسناد باید بخش «دانش و اسناد سازمان» را مخفی کند.
- دکمه بازگشت باید همیشه در Header قابل دسترس باشد.
- جستجوی یک واژه پرتکرار باید:
  - تعداد اسناد منطبق را نشان دهد؛
  - تعداد کل occurrence های متن را نشان دهد؛
  - برای هر سند، تعداد occurrence همان سند را نمایش دهد؛
  - چند محل تطابق را قابل مشاهده کند.
