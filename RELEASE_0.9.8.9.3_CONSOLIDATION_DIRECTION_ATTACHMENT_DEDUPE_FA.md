# Hotfix 0.9.8.9.3 — جهت صحیح Consolidation و حذف تکرار پیوست‌ها

## اصلاحات
- موتور Consolidation فقط روی **سند هدف/مادر** اجرا می‌شود؛ یعنی برای رابطه `amends` متن سند اصلاحی از source خوانده و در target اعمال می‌شود.
- سند اصلاحی دیگر هرگز متن ماده سند مادر را داخل خودش دریافت نمی‌کند.
- برای سند مادر، فقط perspectiveهای ورودی (`amended_by`, `superseded_by`, `extended_by`, `clarified_by`) که واقعاً `targetDocumentRef === currentDocumentId` هستند قابل اعمال‌اند.
- نمایش پیوست‌ها دارای قفل in-flight شد تا MutationObserver نتواند چند پنل همزمان ایجاد کند.
- پیوست‌ها بر اساس artifact id (و fallback هویت فایل) Deduplicate می‌شوند.
- پنل‌های تکراری باقی‌مانده از build قبلی هنگام باز شدن سند پاک می‌شوند.

## تست مرجع
- باز کردن سند 215: الحاقیه مصوبه 291 باید در ماده 19 سند 215 نمایش داده شود.
- باز کردن مصوبه 291: متن ماده 19 سند 215 نباید داخل سند 291 تزریق شود.
- پیوست: فقط یک نوار پیوست و هر artifact فقط یک بار نمایش داده شود.
