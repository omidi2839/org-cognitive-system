# Build 0.9.1 — بانک اسناد سازمان

فایل‌ها را با حفظ مسیر در ریشه مخزن کپی کنید.

## تغییر لازم در public/index.html
در بخش `<head>` بعد از knowledge-workspace-076.css این خط را اضافه کنید:
`<link rel="stylesheet" href="./document-bank-091.css">`

در انتهای `<body>` بعد از knowledge-workspace-076.js این خط را اضافه کنید:
`<script src="./document-bank-091.js"></script>`

## قابلیت‌ها
- حذف نمایشی عبارت Build از سربرگ مخزن
- فارسی‌سازی اعداد در فضای دانش/اسناد
- افزودن کارت «بانک اسناد» به فضای دانش و اسناد سازمان
- جستجو در عنوان، موضوع، نوع سند، مرجع، واحد و نام فایل
- فیلتر طبقه سند، اعتبار، محرمانگی و بازه تاریخ
- نمایش تاریخ صدور/تصویب و متادیتای سند
- آماده‌سازی مدل UI برای کنترل دسترسی

## مرز این Build
جستجوی تمام‌متن واقعی و اجرای سیاست دسترسی در Backend عمداً در این Build نمایشی نشده است؛ برای آن باید شاخص متن + Authorization Resolver سمت سرور اضافه شود. این Build هیچ سند محرمانه‌ای را به‌صورت مصنوعی پنهان/نمایش نمی‌دهد و فقط داده‌ای را نمایش می‌دهد که API فعلی به کاربر برگرداند.
