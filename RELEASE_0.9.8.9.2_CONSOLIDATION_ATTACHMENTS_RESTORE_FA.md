# نسخه 0.9.8.9.2 — بازگردانی Consolidation و پیوست‌های چندگانه

این Hotfix بر پایه معماری پایدار 0.9.8.8.5/0.9.8.9.1 ساخته شده و سه قابلیت کلیدی را تثبیت می‌کند:

1. **Effective Consolidated View**
   - اصلاح/الحاق ثبت‌شده برای سند مادر دوباره داخل متن سند مادر اعمال می‌شود.
   - روابط قدیمی که با جهت معکوس ثبت شده‌اند با اتکا به نوع تغییر، عنوان اصلاحیه/الحاقیه، شماره و تاریخ سند ترمیم می‌شوند.
   - در صورت شکست parser ساختاری، fallback استخراج متن اصلاحیه و fallback مکان‌یابی ماده فعال می‌شود.
   - provenance سند اصلاحی در متن مؤثر حفظ می‌شود.

2. **پیوست‌ها در بالای متن سند**
   - تعداد پیوست‌ها و نام فایل‌ها بالای متن سند نمایش داده می‌شود.
   - اطلاعات ابتدا از Document Bank و در صورت نیاز از Governance API خوانده می‌شود.
   - بازکردن فایل از مسیر امن document-file-access انجام می‌شود.

3. **پیوست‌های نامحدود در UI**
   - در ثبت سند، دکمه «افزودن پیوست دیگر» ردیف جدید می‌سازد.
   - هر ردیف قابل حذف است و همه فایل‌ها در همان ثبت ارسال می‌شوند.
   - همین الگو در ویرایش سند نیز فعال است.

## Deployment safety
- `api/auth-proxy.js` داخل این بسته وجود ندارد.
- `vercel.json` دوباره wildcard API را به `/api/index.js` هدایت می‌کند.
- تعداد فایل‌های top-level داخل پوشه api این بسته: 8.
- صفحه ورود و Session از مسیر `api/knowledge-documents.js` حفظ شده‌اند.
