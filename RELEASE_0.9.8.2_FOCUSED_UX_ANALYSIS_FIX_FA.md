# نسخه 0.9.8.2 — اصلاح متمرکز UX و تحلیل

این نسخه بر مبنای 0.9.8.1 ساخته شده و Deployment-Safe باقی می‌ماند؛ Function جدیدی به `/api` اضافه نشده است.

## اصلاحات
1. دکمه «ویرایش سند» از داخل پنجره نمایش سند حذف شد.
   در بانک اسناد، کنار «مشاهده سند» قرار گرفت و دو دکمه هم‌عرض هستند.
   کاربر بدون permission فقط «مشاهده سند» را می‌بیند.
2. شمارنده کارت‌های اسناد بالادستی/عمومی/بانک اسناد بسیار کوچک و absolute شد تا کارت به ردیف دوم نرود.
3. در متن تلفیقی اصلاحیه‌ها، wrapperهای اداری حتی اگر در یک خط باشند حذف می‌شوند:
   `متن مصوبه:`، `ماده واحده:`، `متن الحاقیه:`، `متن اصلاحیه:`.
4. کلیک «تحلیل اسناد» در سطح Window Capture توسط پنل Collaborative گرفته می‌شود.
   اگر پنل Legacy باز شود، Observer آن را با پنل جدید جایگزین می‌کند.
5. پنل جدید فقط اسناد بالادستی را نمایش می‌دهد و چهار مرحله تحلیل خبرگان را نشان می‌دهد:
   تحلیل مستقل → تحلیل تکمیلی/نقد → جمع‌بندی → تأیید نهایی.
6. تمام اصلاحات 0.9.7.5 ماده/تبصره/بند و append/replace حفظ شده است.
