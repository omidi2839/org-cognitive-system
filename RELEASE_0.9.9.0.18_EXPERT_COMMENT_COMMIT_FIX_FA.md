# 0.9.9.0.18 — رفع گیرکردن ثبت نظر خبرگانی

مشکل مشاهده‌شده:
پس از بازگشایی میز پژوهش و ثبت نظر یا مفهوم جدید، وضعیت روی «در حال ثبت یادداشت خبرگانی…» باقی می‌ماند.

اصلاحات:
- ثبت پاسخ خبرگانی دارای `clientResponseKey` پایدار و idempotent شد.
- در Retry همان شناسه استفاده می‌شود و پاسخ تکراری ساخته نمی‌شود.
- backend بعد از ثبت پاسخ دیگر برای همان درخواست یک خواندن کامل دوم از repository انجام نمی‌دهد و پاسخ سریع مستقیم برمی‌گرداند.
- frontend برای درخواست ثبت timeout کنترل‌شده 18 ثانیه‌ای و یک Retry خودکار دارد.
- دکمه ثبت هنگام درخواست غیرفعال و پس از موفقیت/خطا حتماً آزاد می‌شود.
- پیام موفقیت و خطای واقعی به‌صورت روشن نمایش داده می‌شود.
- در صورت timeout نهایی، کاربر می‌تواند دوباره ثبت را بزند و idempotency از ایجاد رکورد تکراری جلوگیری می‌کند.
