# SINA 0.9.9.2.13

این نسخه چهار اشکال گزارش‌شده را با اصلاح علت اصلی برطرف می‌کند.

## علت اصلی بخشی از اصلاحات قبلی UI
در نسخه‌های قبل، فایل CSS جدید ساخته شده بود اما `index.html` همچنان
`knowledge-governance-0980.css` را بارگذاری می‌کرد. بنابراین قواعد Justify،
رنگ پنل جزئیات و حذف Focus مربع عملاً در مرورگر فعال نمی‌شدند.

در این نسخه فایل فعال به صورت مستقیم و cache-safe به:
`knowledge-governance-099213.css`
تغییر کرده است.

## شمارنده بانک اسناد
شمارنده دیگر از `/knowledge/documents` استفاده نمی‌کند، چون آن endpoint
می‌تواند اسناد داخلی/آماده‌پردازش را نیز شامل شود.
اکنون «بانک اسناد»، «اسناد عمومی» و «اسناد بالادستی» از همان endpoint
`/document-bank` که صفحه بانک اسناد نمایش می‌دهد محاسبه می‌شوند.
بنابراین تعداد کارت باید با موجودی واقعی بانک اسناد همخوان باشد.

## نقشه گلوله‌ای
`tabindex` از گره‌های SVG حذف شد؛ در نتیجه Focus rectangle مشکی مرورگر
دیگر ساخته نمی‌شود.

## پنل جزئیات
مرکزیت دانشی، اولویت شناختی و تعداد روابط هم با کلاس و هم با style مستقیم
سه رنگ متفاوت دارند تا وابسته به cache یا specificity نباشند.

## متن بلند فارسی
کارت‌های دانش و مفاهیم:
- RTL
- Justify
- text-align-last:right
- ارتفاع محدود
- اسکرول داخلی

دارند و style مستقیم نیز به markup اضافه شده تا نتیجه قطعی باشد.
