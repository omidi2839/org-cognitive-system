# 0.9.9.0.23 — ویرایش سند + نمایش ملغی + ثبت مستقیم

1. ویرایش سند:
   resolver ویرایش علاوه بر document id، شناسه normalized document و artifact را نیز به سند اصلی resolve می‌کند.
   ممیزی با شناسه canonical سند ثبت می‌شود.

2. اثر حقوقی ملغی:
   repeals/repealed_by وارد Consolidation سند مادر شد.
   متن صریح ثبت‌شده در رابطه حقوقی، زیر ماده/تبصره هدف درج می‌شود و با رنگ قرمز نمایش داده می‌شود.
   مثال: ماده ۲۰، تبصره ۱ → متن اثر ملغی دقیقاً زیر همان تبصره.

3. ثبت مستقیم:
   مشکل overwrite شدن فرم توسط پاسخ دیررس repo() رفع شد.
   دکمه ثبت اسناد بالادستی/عمومی تا پایان load صفحه legacy صبر می‌کند و سپس مستقیماً فرم مخصوص همان نوع سند را باز می‌کند.
