# Release 0.9.9.0 — Explicit Legal Effects + Document Retrieval Commands

این نسخه دو محور اصلی دارد:

## الف) قرارداد صریح ارتباط حقوقی
- نوع ارتباط کاربرپسند به چهار گزینه محدود شد: **اصلاحیه، الحاقیه، ملغی، استفسار**.
- فیلد «نوع تغییر» از تجربه کاربر حذف شد و فقط برای سازگاری داخلی مقداردهی می‌شود.
- برای هر اثر حقوقی، کاربر **ماده، تبصره/بند و متن کامل اثر حقوقی** را ثبت می‌کند.
- متن ثبت‌شده منبع قطعی Consolidation است و موتور دیگر برای اصلاحیه/الحاقیه کوتاه از متن سند دوم حدس نمی‌زند.
- رنگ‌ها: اصلاحیه سبز، الحاقیه آبی، ملغی قرمز، استفسار نارنجی.
- اگر تبصره/بند وارد شود، درج فقط در همان locator انجام می‌شود؛ در صورت پیدا نشدن locator، موتور تغییر را اشتباهاً ته ماده نمی‌چسباند.
- دستورالعمل‌ها و اسناد اجرایی مفصل همچنان reference-only و لینک‌شده باقی می‌مانند.

## ب) Document Retrieval & Command Suggestion Engine V1
- زیر میزکار شناختی یک بخش «جستجو در بانک اسناد» با فرمان‌های پیشنهادی اضافه شد.
- هنگام تایپ، پیشنهادهای نزدیک به عبارت کاربر به‌صورت autocomplete معنایی نمایش داده می‌شود.
- پیشنهادها از موضوع، زیرموضوع، مرجع تصویب، نوع سند، شماره جلسه و شماره سند واقعی بانک تولید می‌شوند.
- جستجوهای پشتیبانی‌شده: موضوع/زیرموضوع، تاریخ، مرجع تصویب، شماره مصوبه/سند، شماره جلسه، نوع سند، وضعیت اعتبار، جستجوی متن کامل، روابط اصلاحیه/الحاقیه/استفسار/ملغی و ترکیب چند فیلتر.
- API `document-query.js` برای این فیلترهای ساختاری ارتقا یافت.

تعداد فایل‌های top-level پوشه `api`: **8** و `auth-proxy.js` وجود ندارد.
