# 0.9.9.1.1 — SINA Evidence & Tool Calling V1

Baseline: 0.9.9.1.0 روی 0.9.9.0.35

## قابلیت
فرمان‌های مرتبط با اسناد در میزکار شناختی ابتدا به سینا ارسال می‌شوند. مدل اجازه دسترسی مستقیم به DB ندارد و فقط سه Tool داخلی دارد:
1. `search_documents`
2. `get_document`
3. `get_document_relations`

سینا موظف است قبل از پاسخ درباره سازمان، ابتدا بانک اسناد را جستجو کند. شواهد بازیابی‌شده مستقل از متن مدل در UI نمایش داده می‌شوند.

## رفتار ایمن
- Search قطعی قبلی حذف نشده و در صورت خطای AI به صورت خودکار fallback است.
- کلید API فقط در Backend است.
- حداکثر ۴ دور Tool Calling.
- متن کامل هر سند برای مدل سقف دارد.
- پاسخ سازمانی بدون Evidence باید کمبود شواهد را اعلام کند.
- نتایج جستجو مانند نسخه 0.9.9.0.35 موقت هستند و با خروج از میزکار پاک می‌شوند.

## Endpoint
POST `/api/v1/ai/command`
Body:
`{"question":"اسناد مربوط به حقوق و مزایای کارکنان را بررسی کن و جمع‌بندی بده"}`

## Environment
همان متغیرهای 0.9.9.1.0 کافی‌اند:
- OPENAI_API_KEY
- SINA_AI_ENABLED=true
- SINA_AI_PROVIDER=openai
- SINA_AI_MODEL اختیاری (پیش‌فرض gpt-5.6-terra)
