# Feature 0.9.4.0

اصلاحات این نسخه:

1. مشکل ماندن Popup روی «در حال دریافت اصلاحات و سوابق سند…»
   - علت: MutationObserver نسخه قبل با تغییر DOM خودش دوباره درخواست می‌فرستاد و حلقه ایجاد می‌کرد.
   - در این نسخه Observer شبکه اصلاحات حذف شده و هر Preview فقط یک بار سوابق را می‌خواند.
   - Timeout هشت‌ثانیه‌ای اضافه شده تا UI هرگز بی‌نهایت در حالت Loading نماند.

2. ثبت ارتباط اسناد از Popup حذف شد.
   - Popup فقط خروجی و سوابق را نشان می‌دهد.
   - فیلد «ارتباط با اسناد قبلی» به فرم «افزودن سند» منتقل شده است.
   - بعد از ثبت موفق سند، Relation به همان سند جدید متصل و در PostgreSQL ذخیره می‌شود.

3. مخزن اسناد دیگر لیست اسناد را نمایش نمی‌دهد.
   - فقط کارت‌های آماری باقی می‌مانند.
   - کارت «تعداد سند بالادستی/عمومی» قابل کلیک است و کاربر را مستقیم به بانک اسناد با فیلتر همان نوع می‌برد.
   - کارت «نیازمند بررسی انسانی» قابل کلیک است و به «تحلیل اسناد» می‌رود.

4. تاریخ‌های میلادی به UI شمسی تبدیل شدند.
   - فرم افزودن سند: تاریخ صدور و پایان اعتبار با تقویم جلالی.
   - فیلترهای بانک اسناد: از تاریخ / تا تاریخ با تقویم جلالی.
   - مقدار ارسالی Backend همچنان ISO Gregorian است تا منطق Date و Query دیتابیس نشکند.

## نصب
فایل‌های زیر جایگزین فایل‌های موجود 0.9.3.1 شوند:
- public/document-amendments-093.js
- public/document-amendments-093.css

در public/index.html فقط Query Version را از 0.9.3.1 به 0.9.4.0 تغییر دهید:
- document-amendments-093.css?v=0.9.4.0
- document-amendments-093.js?v=0.9.4.0

API و vercel.json نسخه 0.9.3.1 را نگه دارید.
