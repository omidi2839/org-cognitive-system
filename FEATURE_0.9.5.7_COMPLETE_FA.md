# Feature 0.9.5.7 COMPLETE

اصلاحات:
- پیام ثبت سند فقط «سند با موفقیت ثبت شد.» با رنگ سبز است.
- در خطا فقط «در ثبت سند مشکلی به وجود آمد.» با رنگ قرمز نمایش داده می‌شود.
- پیشنهاد حوزه موضوعی: عنوان سند وزن بسیار بالاتر دارد، سپس سوتیتر/پاراگراف‌های ابتدایی، سپس متن کامل.
- هایلایت عبارت جستجو بعد از بازسازی ساختاری DOCX دوباره اعمال می‌شود.
- درصدها به صورت ریاضی صحیح مثل «۹٪» نمایش داده می‌شوند؛ حالت «٪۹» اصلاح می‌شود.
- کسرهای Office Math و جدول‌های layout که فقط کسر هستند، به صورت کسر واقعی نمایش داده می‌شوند.
- ادغام افقی/عمودی سلول‌های Word با colspan/rowspan حفظ می‌شود.

برای بازیابی merge/fraction روی اسناد Word قدیمی، فایل باید دوباره پس از نصب این نسخه وارد شود.

Build:
window.__DOCUMENT_MEETING_COMMAND_BUILD__
=> 0.9.5.7
