# Release 0.9.8.9.4

این Hotfix چهار مسئله را همزمان اصلاح می‌کند:

1. **پیوست‌ها**
   - هویت سند فعال به خود modal متصل شد تا پیوست سند مادر هنگام رفتن به سند اصلاحی/الحاقی نشت نکند.
   - بازکردن پیوست از مسیر same-origin و احرازشده انجام می‌شود و فایل از Private Blob با SDK خوانده و stream می‌شود.
   - برای PDF نمایش inline و برای Word دانلود امن انجام می‌شود.

2. **پایداری ثبت سند**
   - سقف Base64 امن‌تر شد و transport به صورت ترکیبی per-file کار می‌کند.
   - فایل‌های کوچک داخل بودجه امن Function ارسال می‌شوند و فایل‌های بزرگ‌تر Direct Blob با retry بیشتر ارسال می‌شوند.

3. **موضوع‌بندی**
   - تحلیل موضوعی دیگر با انتخاب فایل یا تغییر موضوع به‌صورت خودکار درخواست سنگین نمی‌فرستد.
   - دکمه تحلیل، برای فایل‌های بزرگ از پیشنهاد سریع metadata-only استفاده می‌کند و برای فایل‌های کوچک متن فایل را تحلیل می‌کند.
   - شکست سرویس پیشنهاد موضوع مانع ثبت سند نیست.

4. **Consolidation**
   - اصلاحیه‌های کوتاه همچنان در محل ماده هدف inline می‌شوند.
   - اگر سند مرتبط یک دستورالعمل/شیوه‌نامه/سند اجرایی مفصل باشد، متن چندصفحه‌ای آن داخل سند مادر کپی نمی‌شود.
   - زیر ماده هدف فقط اعلام می‌شود که آن ماده دارای سند اجرایی/دستورالعمل مصوب است و لینک سند مرتبط نمایش داده می‌شود.
   - رابطه `implements` نیز به‌صورت reference-only در نمای سند مادر پشتیبانی می‌شود.

تعداد فایل‌های سطح بالای `api/`: **8**. فایل `api/auth-proxy.js` در بسته وجود ندارد.
