# 0.9.9.0.26 — Stable Relations + Direct Registration + DOM Back

این نسخه عمداً از 0.9.9.0.23 به‌عنوان آخرین خط پایدار روابط حقوقی ساخته شده است و تغییرات 0.9.9.0.24/25 که باعث حذف بخشی از Consolidation و سوابق می‌شدند وارد آن نشده‌اند.

## ثبت مستقیم سند
- CTA جدید با شناسه مستقل ساخته می‌شود و CTAهای قبلی 0.9.9.0.22–25 حذف می‌شوند.
- قبل از رخداد click، کارت موقتاً `data-capability` و کلاس `capability-card` را از دست می‌دهد؛ بنابراین capture handlerهای قدیمی نمی‌توانند صفحه داخلی کارت را باز کنند.
- فرم `#k76form` مستقیماً ساخته می‌شود؛ صفحه داخلی کارت مسیر ثبت نیست.

## سند مادر و روابط
- موتور Consolidation نسخه 0.9.9.0.23 حفظ شده است.
- هیچ event جدیدی برای reset/re-enrich متن سند اضافه نشده است.
- هنگام رفتن از سند مادر به اصلاحیه/الحاقیه/ملغی، DOM کامل سند مادر (با تمام استنادات، سوابق، اصلاحات و handlerها) از صفحه جدا و در حافظه نگهداری می‌شود.
- «بازگشت به سند قبلی» همان DOM قبلی را عیناً برمی‌گرداند؛ سند مادر دوباره fetch/re-render نمی‌شود.
- بنابراین همه اصلاحات/الحاقات/الغائات و سوابق دقیقاً همان‌طور که قبل از رفتن دیده می‌شدند باقی می‌مانند.
- دکمه بازگشت در نوار مستقل زیر metadata است و فضای دکمه‌های چاپ/افزودن به اسناد من را اشغال نمی‌کند.

## شماره مصوبه
شماره مصوبه/سند در کنار لینک سند اصلاحی/الحاقی/ملغی نمایش داده می‌شود.
