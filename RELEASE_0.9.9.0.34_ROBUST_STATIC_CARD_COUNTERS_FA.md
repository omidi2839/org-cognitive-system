# 0.9.9.0.34 — شمارنده پایدار کارت‌های اسناد

- شمارنده کارت‌های «اسناد بالادستی» و «اسناد عمومی» دیگر به renderer عمومی وابسته نیست.
- خود overlay ثبت مستقیم، تعداد اسناد را از `/api/v1/knowledge/documents` می‌خواند.
- تعداد upstream و general مستقل محاسبه می‌شود.
- مقدار شمارنده cache می‌شود و حداکثر هر ۳۰ ثانیه refresh می‌شود.
- MutationObserver فقط مقدار cache شده را روی DOM اعمال می‌کند؛ request loop ایجاد نمی‌شود.
- CSS نمایش badge را قطعی می‌کند.
- رفع کلیک نسخه 0.9.9.0.33 و تمام اصلاحات قبلی حفظ شده‌اند.
