# SINA 0.9.9.2.5 — DOCX Header Metadata + Large Bulk + Stage Flow Fix

این نسخه چهار اصلاح هدفمند در ورود دسته‌جمعی اسناد دارد:

1. **خواندن سربرگ و پابرگ Word**
   - فایل‌های `word/header*.xml` و `word/footer*.xml` از DOCX خوانده می‌شوند.
   - متن سربرگ/پابرگ قبل و بعد از متن اصلی وارد متن نرمال‌شده می‌شود.
   - برای طبقه‌بندی، سربرگ و پابرگ به‌عنوان محل مهم شناسنامه سند در اولویت قرار می‌گیرند.

2. **شماره و تاریخ**
   - شماره سند، شماره جلسه، تاریخ جلسه و تاریخ ابلاغ در صورت نبود خروجی مدل، با قواعد محافظه‌کارانه از سربرگ استخراج می‌شوند.
   - تاریخ‌های شمسی/میلادی با `/` یا `-` به فرم استاندارد `YYYY-MM-DD` نرمال می‌شوند.

3. **فایل‌های بزرگ**
   - فایل‌های بزرگ فقط یک‌بار به Private Blob آپلود می‌شوند.
   - Backend همان Blob را در صورت تأخیر انتشار چند بار می‌خواند؛ دیگر در هر Retry یک Blob تازه ساخته نمی‌شود.
   - مسیر Base64 برای فایل‌های کوچک/متوسط حفظ شده است.

4. **مراحل ورود دسته‌جمعی**
   - وضعیت مراحل ۱ انتخاب، ۲ پردازش و شناسایی، ۳ بازبینی و ۴ ثبت نهایی به‌صورت واقعی با جریان عملیات جلو می‌رود.
   - مراحل انجام‌شده علامت‌گذاری می‌شوند.

تمام تغییرات UI و درختواره نسخه‌های قبل حفظ شده‌اند.
