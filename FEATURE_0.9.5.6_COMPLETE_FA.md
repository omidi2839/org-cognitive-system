# Feature 0.9.5.6 COMPLETE — Calendar / Digits / Alignment / Fractions

بسته کامل و بدون ویرایش دستی.

اصلاحات:
- کلیک روی ماه قبل/بعد دیگر تقویم را نمی‌بندد.
- کنترل «سال قبل / سال بعد» به تقویم شمسی اضافه شد.
- اعداد در نمایش متن سند به رقم فارسی یکسان‌سازی می‌شوند.
- تراز پاراگراف‌ها و سلول‌های Word (center/right/left/justify) از DOCX خوانده و در Popup حفظ می‌شود.
- کسرهای Office Math در Word به صورت fraction واقعی نمایش داده می‌شوند، نه ساختار جدولی/متن تخت.
- جدول‌های واقعی Word همچنان به شکل جدول نمایش داده می‌شوند.
- قابلیت‌های نسخه‌های قبل حفظ شده‌اند.

نکته: برای اسناد Word قدیمی که قبل از 0.9.5.6 وارد شده‌اند، جهت بازیابی تراز و کسرهای ساختاریافته لازم است فایل دوباره وارد شود.

Build marker:
window.__DOCUMENT_MEETING_COMMAND_BUILD__
=> 0.9.5.6
