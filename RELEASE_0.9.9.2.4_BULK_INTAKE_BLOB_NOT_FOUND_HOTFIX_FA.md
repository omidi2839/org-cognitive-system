# SINA 0.9.9.2.4 — Bulk Intake Blob Not Found Hotfix

## مشکل
در ورود دسته‌جمعی اسناد، پس از PUT موفق فایل به Private Vercel Blob، API بلافاصله همان فایل را برای استخراج متن می‌خواند. در برخی درخواست‌ها Blob هنوز برای خواندن سروری قابل مشاهده نبود و خطای زیر ایجاد می‌شد:

`Vercel Blob object was not found.`

## اصلاح
- فایل‌های تا 2.6MB در ورود دسته‌جمعی مستقیماً در همان درخواست API به صورت Base64 امن منتقل می‌شوند و دیگر به read-after-write فوری Blob وابسته نیستند.
- فایل‌های بزرگ‌تر همچنان از Direct Blob استفاده می‌کنند، اما قبل از ثبت سروری یک زمان کوتاه برای انتشار Blob داده می‌شود و تا 3 بار تلاش انجام می‌شود.
- منطق تشخیص تکرار، طبقه‌بندی، بازبینی و ثبت نهایی بدون تغییر حفظ شده است.
- هیچ تغییری در نسخه‌های قبلی UI، درختواره، ابر کلمات و ناوبری ایجاد نشده است.
