import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {MemoryRepository} from '../src/infrastructure/memoryRepository.js';import {MockAIGateway} from '../src/ai/mockGateway.js';import {CognitiveService} from '../src/application/service.js';
const actor={organizationId:'ORG:TYPO',personId:'PER:1',roles:['expert'],correlationId:'T'};const setup=()=>new CognitiveService(new MemoryRepository(),new MockAIGateway());
test('Persian typography system uses Vazirmatn-first stack',()=>{const h=fs.readFileSync('public/index.html','utf8'),c=fs.readFileSync('public/styles.css','utf8');assert.match(h,/Vazirmatn/);assert.match(c,/--fa-font/);});
test('cognitive brief produces a specialized workspace',async()=>{const s=setup();const r=await s.executeCommand(actor,{sessionId:'B1',text:'یک جمع‌بندی شناختی بده'});assert.equal(r.intent,'workspace.cognitive_brief');assert.equal(r.workspace.componentType,'cognitive_brief');assert.ok(r.items.length>=2);});
