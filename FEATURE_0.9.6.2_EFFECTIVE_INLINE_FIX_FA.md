# Feature 0.9.6.2 — اصلاح قطعی درج اصلاحیه و ترتیب شماره مواد

علت اینکه 0.9.6.1 اصلاحیه را درج نمی‌کرد:
فیلد ماده در رابطه می‌توانست به صورت «ماده ۱۹» ذخیره شده باشد، اما کد قبلی آن را مستقیم Number() می‌کرد؛
بنابراین هدف NaN می‌شد و ماده ۱۹ پیدا نمی‌شد.

اصلاحات:
- شماره ماده از هر قالبی مثل «۱۹»، «ماده ۱۹»، «ماده ۱۹ · بند ۵» استخراج می‌شود.
- اصلاحیه فقط در انتهای ماده هدف و قبل از ماده بعدی درج می‌شود.
- متن واقعی سند اصلاحی با کلیدواژه‌های changeItems.description پیدا می‌شود و جمله دستوری «یک بند اضافه شود...» نمایش داده نمی‌شود.
- شماره مواد موجود در اسناد قدیمی با تحلیل توالی اصلاح می‌شوند؛ نمونه ۸۱، ۹۱، ۰۲ به ۱۸، ۱۹، ۲۰ برمی‌گردد.
- اعداد پس از اصلاح با bidi isolate نمایش داده می‌شوند تا مرورگر دوباره ترتیب آنها را برعکس نکند.
- هیچ API Function جدیدی اضافه نشده است.
