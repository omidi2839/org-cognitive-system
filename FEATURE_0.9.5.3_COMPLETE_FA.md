# Feature 0.9.5.3 COMPLETE — Smart Document Metadata & Canonical Topics

بسته کامل است و هیچ ویرایش دستی لازم ندارد.

## اصلاحات فرم ثبت سند
- تاریخ برگزاری جلسه به ورود شمسی تبدیل شد؛ مقدار داخلی همچنان ISO میلادی ذخیره می‌شود تا Backend استاندارد بماند.
- تاریخ صدور و تاریخ پایان اعتبار هم در رابط شمسی نمایش داده می‌شوند.
- «تاریخ ابلاغ سند» اضافه شد.
- «نوع جلسه» به صورت انتخاب از فهرست استاندارد اضافه شد.
- اگر وضعیت اعتبار «معتبر» باشد، تاریخ پایان اعتبار پنهان و پاک می‌شود.
- اگر وضعیت اعتبار غیر از «معتبر» باشد، تاریخ پایان/قطع اعتبار نمایش داده می‌شود و الزامی است.

## حوزه موضوعی
- فیلد آزاد حذف شد.
- کاربر نمی‌تواند هر عبارت دلخواهی به عنوان موضوع ثبت کند.
- پس از انتخاب فایل، سامانه متن سند را با `parseArtifact` می‌خواند.
- حوزه‌های موضوعی استاندارد را امتیازدهی می‌کند و تا سه موضوع پیشنهاد می‌دهد.
- کاربر فقط از فهرست canonical انتخاب می‌کند.
- موضوعات موجود اسناد قبلی نرمال و deduplicate می‌شوند؛ بنابراین ثبت عبارات تکراری با نگارش متفاوت کاهش می‌یابد.

## فیلدهای جدید metadata
- meetingType
- meetingDate
- meetingNumber
- promulgationDate
- subjectArea (canonical)
- validUntil

## Endpoint جدید
POST /api/v1/knowledge/topic-suggestions

## نصب
فقط محتویات ZIP را با حفظ ساختار روی پروژه کپی/جایگزین کنید و Deploy بگیرید.
هیچ تغییر دستی در index.html یا vercel.json لازم نیست.

Build marker:
window.__DOCUMENT_MEETING_COMMAND_BUILD__
=> 0.9.5.3
