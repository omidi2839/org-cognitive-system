# 0.9.9.0.21 — رفع خطای «نوع تغییر الزامی است»

## مشکل
در فرم ثبت سند، کاربر فقط نوع اثر حقوقی را انتخاب می‌کند:
اصلاحیه / الحاقیه / ملغی / استفسار.
فیلد قدیمی «نوع تغییر» از UI حذف شده و باید به‌صورت داخلی از نوع رابطه مشتق شود.
در حالت «ملغی»، کنترل Legacy پنهان مقدار سازگار نداشت و Validator قدیمی پیام «نوع تغییر الزامی است» می‌داد.

## اصلاح
- مقدار داخلی نوع تغییر به‌صورت قطعی مشتق می‌شود:
  - اصلاحیه → اصلاح متن
  - الحاقیه → الحاق
  - ملغی → لغو
  - استفسار → تفسیر/توضیح
- اگر کنترل Legacy یک select باشد و option متناظر نداشته باشد، option سازگاری به‌صورت پنهان اضافه می‌شود.
- required کنترل قدیمی حذف می‌شود.
- قبل از click روی ثبت، قبل از submit و قبل از reportValidity مقدار داخلی sync می‌شود.
- فیلد «نوع تغییر» همچنان به UI برنمی‌گردد؛ قرارداد فعلی UX حفظ شده است.
