import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync('src/application/cognitiveDocumentUnderstanding077.js','utf8');test('zones',()=>{for(const x of ['documentZone','preamble_heading','eligibleForConceptualization'])assert.ok(s.includes(x),x)});test('precision',()=>{for(const x of ['verbBoundaries','candidateScore','structural_semantic_candidate'])assert.ok(s.includes(x),x)});test('questions',()=>{assert.ok(s.includes('single_concept_definition'));assert.ok(!s.includes('آیا مفاهیم کلیدی استخراج‌شده از این گزاره کامل'));assert.ok(!s.includes('کدام‌یک از این مفاهیم مستقل‌اند'))});