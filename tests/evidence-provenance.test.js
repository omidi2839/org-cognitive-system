import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {MemoryRepository} from '../src/infrastructure/memoryRepository.js';import {MockAIGateway} from '../src/ai/mockGateway.js';import {CognitiveService} from '../src/application/service.js';
const actor={organizationId:'ORG:EVID',personId:'PER:1',roles:['expert'],correlationId:'E'};const setup=()=>new CognitiveService(new MemoryRepository(),new MockAIGateway());
test('focus items expose provenance without inventing evidence',async()=>{const s=setup();const f=await s.buildCognitiveFocusQueue(actor);for(const i of f.items){assert.ok(i.provenance);assert.ok(Array.isArray(i.provenance.reasoning));assert.ok(['available','reference_only'].includes(i.provenance.evidenceStatus));}});
test('frontend exposes why/evidence chain',()=>{const j=fs.readFileSync('public/app.js','utf8'),c=fs.readFileSync('public/styles.css','utf8');assert.match(j,/مشاهده زنجیره شواهد/);assert.match(j,/پیشنهاد سامانه جایگزین تصمیم سازمانی نیست/);assert.match(c,/\.evidence-chain/);});
