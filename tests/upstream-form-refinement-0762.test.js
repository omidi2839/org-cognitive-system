import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('upstream form refinement',()=>{const j=fs.readFileSync('knowledge-workspace-076.js','utf8'),p=fs.readFileSync('public/knowledge-workspace-076.js','utf8');assert.equal(j,p);assert.ok(j.includes("'چارچوب','ضوابط'"));assert.ok(!j.includes("'سند بالادستی'"));assert.ok(j.includes('دامنه سازمانی'));assert.ok(j.includes('واحد سازمانی'));assert.ok(j.includes('/api/v1/organization/units'));assert.ok(j.includes('organizationalUnitRef'));assert.ok(j.includes('<option value="confidential">محرمانه</option><option value="public">عمومی</option>'));});