import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {MemoryRepository} from '../src/infrastructure/memoryRepository.js';import {MockAIGateway} from '../src/ai/mockGateway.js';import {CognitiveService} from '../src/application/service.js';
const actor={organizationId:'ORG:DB',personId:'PER:DB',roles:['expert']};
test('decision brief packages evidence without creating a decision',async()=>{const repo=new MemoryRepository();await repo.mutate(db=>db.workItems.push({id:'WORK:DB-1',organizationId:actor.organizationId,assigneeRef:actor.personId,title:'موضوع بسته تصمیم',status:'open',priority:3,dueAt:'امروز'}));const s=new CognitiveService(repo,new MockAIGateway());const r=await s.buildDecisionBrief(actor,{ref:'WORK:DB-1'});assert.equal(r.fabricated,false);assert.equal(r.governance.decisionCreated,false);assert.equal(r.governance.authorityGranted,false);assert.equal(r.governance.humanJudgmentRequired,true);assert.ok(r.brief.executiveQuestion);assert.ok(Array.isArray(r.brief.preDecisionChecklist));});
test('decision brief renderer and API exist',()=>{const j=fs.readFileSync('public/app.js','utf8'),a=fs.readFileSync('api/index.js','utf8');assert.match(j,/decision_brief/);assert.match(j,/Decision Brief Package/);assert.match(a,/workspace\/decision-brief/);});
