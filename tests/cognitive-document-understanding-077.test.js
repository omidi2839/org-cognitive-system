import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('0.7.7 lifecycle and understanding',()=>{const u=fs.readFileSync('knowledge-workspace-076.js','utf8'),s=fs.readFileSync('src/application/cognitiveDocumentUnderstanding077.js','utf8'),a=fs.readFileSync('api/index.js','utf8');for(const x of ['در انتظار تحلیل','تحلیل‌شده / در انتظار بررسی','تحلیل تأییدشده','مشاهده و بررسی تحلیل','مشاهده دانش تأییدشده','ایجاد نسخه جدید تحلیل'])assert.ok(u.includes(x));for(const x of ['concepts','relations','claims','questions','clarifications','forceNewVersion','awaiting_human_clarification','approved'])assert.ok(s.includes(x));assert.ok(a.includes('/cognitive-analysis'));assert.ok(a.includes('/cognitive-questions'));});
