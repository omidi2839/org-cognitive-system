import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('0.7.6 integration',()=>{const h=fs.readFileSync('index.html','utf8'),ph=fs.readFileSync('public/index.html','utf8'),j=fs.readFileSync('workspace-shell-075.js','utf8'),pj=fs.readFileSync('public/workspace-shell-075.js','utf8'),k=fs.readFileSync('knowledge-workspace-076.js','utf8'),pk=fs.readFileSync('public/knowledge-workspace-076.js','utf8');assert.equal(h,ph);assert.equal(j,pj);assert.equal(k,pk);assert.ok(h.includes('knowledge-workspace-076.css'));assert.ok(h.includes('knowledge-workspace-076.js'));assert.ok(j.includes("'اسناد بالادستی','اسناد عمومی','تحلیل اسناد'"));assert.ok(k.includes("b.dataset.capability==='اسناد بالادستی'"));assert.ok(k.includes("b.dataset.capability==='اسناد عمومی'"));assert.ok(k.includes('/api/v1/documents/upload'));});
