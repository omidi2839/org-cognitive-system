# Hotfix 0.9.9.0.2

این نسخه دو ریشه اصلی Regression را اصلاح می‌کند:

## 1. ثبت سند
- Rewrite عمومی Vercel از الگوی نامطمئن `/api/(.*)` به الگوی مستند `/api/v1/:path*` تغییر کرد.
- برای `/api/v1/documents/upload` یک Rewrite صریح به `api/index.js` اضافه شد.
- هر کلیک ثبت، یک `clientRequestId` ثابت دارد و Retry همان درخواست سند تکراری ایجاد نمی‌کند.
- کلیک‌های همزمان مسدود شده‌اند.
- بودجه مسیر same-origin برای فایل‌های کوچک به 2.6MB بازگشت.
- اگر Direct Blob به `Blob object not found` برسد و اندازه فایل اجازه دهد، همان ثبت به‌صورت خودکار یک‌بار از مسیر Function بازیابی می‌شود.
- دکمه ثبت زودتر از پایان واقعی درخواست آزاد نمی‌شود.

## 2. ارتباط حقوقی صریح
- بعد از ثبت موفق سند، شناسه واقعی سند مستقیماً از پاسخ Upload گرفته می‌شود.
- جزئیات کامل `relationType + relatedDocumentId + article + clause + description` با تأخیر کوتاه روی همان Relation upsert می‌شود.
- این مرحله دیگر به متن Status یا حدس MutationObserver وابسته نیست.
- برای هر اثر حقوقی، ماده و متن کامل الزامی است.
- Consolidation نسخه 0.9.9.0 حفظ شده: اصلاحیه سبز، الحاقیه آبی، ملغی قرمز، استفسار نارنجی؛ دستورالعمل مفصل reference-only.

هیچ فایل API جدیدی اضافه نشده است.
