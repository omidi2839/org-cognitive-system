# Release 0.9.8.8.5 — Resilient Document Upload

این نسخه مسیر ثبت فایل را در برابر قطع اتصال مستقیم مرورگر به Vercel Blob مقاوم می‌کند.

- برای مجموعه فایل‌های کوچک تا 2.75MB خام، فایل مستقیماً در همان درخواست ثبت سند به Function ارسال می‌شود؛ Base64 با مسیر سازگار قبلی پردازش می‌شود و سپس Storage server-side انجام می‌شود.
- برای فایل‌های بزرگ‌تر، Direct Blob حفظ شده و تا سه بار با backoff کوتاه تلاش می‌شود.
- همین سیاست برای ثبت سند جدید و جایگزینی/پیوست فایل در فرم ویرایش اعمال شده است.
- هیچ API Function جدیدی ایجاد نشده است.
- Supabase، Private Blob و OIDC تغییر معماری نداده‌اند.
- سقف 2.75MB عمداً پایین‌تر از حد 4.5MB payload تابع انتخاب شده تا سربار Base64 و metadata حاشیه امن داشته باشد.
