# نسخه 0.9.8.6 — Direct-to-Blob Upload

هدف این نسخه حذف فایل‌های Base64 از مسیر اصلی Vercel Function است.

## جریان جدید
1. مرورگر برای هر فایل Word/PDF یک URL کوتاه‌عمر و محدود به PUT دریافت می‌کند.
2. خود فایل مستقیماً از مرورگر به Vercel Private Blob ارسال می‌شود.
3. API سامانه فقط `blobUrl/blobPathname` و Metadata را دریافت می‌کند.
4. هنگام استخراج و نرمال‌سازی، Backend فایل خصوصی را از Blob می‌خواند.
5. فایل اصلی و پیوست‌ها همچنان از نظر Repository/Provenance مستقل نگهداری می‌شوند.

## مزیت
- Base64 دیگر وارد JSON درخواست ثبت سند نمی‌شود.
- فایل از Browser به Vercel Function و سپس دوباره به Blob عبور نمی‌کند.
- محدودیت 4.5MB Function payload عملاً از مسیر فایل حذف می‌شود.
- چند پیوست نیز به‌صورت مستقل و مستقیم Upload می‌شوند.

## نیاز محیط
- پروژه باید Vercel Blob متصل داشته باشد.
- Private Blob/OIDC یا `BLOB_READ_WRITE_TOKEN` باید برای پروژه فعال باشد.
- `@vercel/blob` در این Release به `^2.6.1` ارتقا یافته است.

## سازگاری
مسیر Base64 قدیمی در Backend حذف نشده و برای داده/ابزارهای قدیمی قابل استفاده است، اما UI جدید به‌صورت پیش‌فرض فقط Direct Upload را استفاده می‌کند.
