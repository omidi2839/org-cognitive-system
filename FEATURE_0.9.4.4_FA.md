# Feature 0.9.4.4 — Relation UX & Multi-Clause Amendment

این نسخه منطق Relation تثبیت‌شده 0.9.4.3 را حفظ می‌کند و فقط UX و مدل جزئیات اصلاحیه را توسعه می‌دهد.

## فرم ثبت سند
نوع ارتباط به سه گزینه اصلی حقوقی ساده شده است:
- اصلاحیه سند
- ملغی کردن سند
- استفسار سند

«نوع تغییر» در صورت انتخاب ارتباط الزامی است.

برای مواد و تبصره‌ها یک Repeater اضافه شده:
- ماده
- بند / تبصره
- شرح مستقل تغییر
- دکمه «+ افزودن ماده / تبصره»
- امکان حذف هر مورد

یک اصلاحیه می‌تواند هر تعداد ماده/تبصره را در `changeItems[]` ذخیره کند.

## Popup
بخش «اصلاحات و سوابق این سند» در بالای Popup با:
- تیتر بزرگ‌تر و پررنگ
- زمینه و Border متمایز
- Badge وضعیت
- کارت مجزا برای هر Relation
- نمایش جزءبه‌جزء مواد و تبصره‌ها و شرح آنها
نمایش داده می‌شود.

## نصب
1. فایل `api/document-relations.js` را جایگزین کنید.
2. فایل‌های زیر را به public اضافه کنید:
   - `document-relations-ui-0944.js`
   - `document-relations-ui-0944.css`
3. در index.html، فایل Preview قدیمی 0.9.4.3 را حذف کنید و این دو خط را قرار دهید:
   `<link rel="stylesheet" href="./document-relations-ui-0944.css?v=0.9.4.4">`
   `<script src="./document-relations-ui-0944.js?v=0.9.4.4"></script>`
4. `document-amendments-093.js?v=0.9.4.0` باید همچنان قبل از 0944 لود شود، چون 0944 UI ثبت ارتباط آن را توسعه می‌دهد.

Console:
`window.__DOCUMENT_RELATION_UI_BUILD__`
باید `0.9.4.4` برگرداند.

روابط قبلی حذف نمی‌شوند؛ Backend هنگام GET رکوردهای قدیمی را نیز به ساختار جدید سازگار می‌کند.
