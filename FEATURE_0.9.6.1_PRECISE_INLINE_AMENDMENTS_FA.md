# Feature 0.9.6.1 — اصلاحیه دقیق در متن سند اصلی

در این نسخه:
- باکس سبز نسخه 0.9.6.0 حذف شده است.
- سامانه برای هر اصلاحیه، متن خود سند اصلاحی را می‌خواند.
- با استفاده از `changeItems.description` و کلیدواژه‌های آن، بخش واقعیِ اصلاح/الحاق در سند اصلاحی پیدا می‌شود.
- بخش استخراج‌شده دقیقاً در انتهای ماده هدف و قبل از ماده بعدی قرار می‌گیرد.
- متن اصلاحیه فقط با رنگ متفاوت نمایش داده می‌شود و از نظر بصری جزئی از متن سند اصلی است.
- یک لینک بسیار کوچک منبع برای provenance باقی می‌ماند و سند اصلاحی را باز می‌کند.
- اگر ماده هدف در سند اصلی پیدا نشود، اصلاحیه دیگر به اشتباه به انتهای کل سند اضافه نمی‌شود.
- نمایش عددها با `unicode-bidi:isolate-override` و `direction:ltr` پایدار شده تا ۱۷ به صورت ۷۱ دیده نشود.
- هیچ API Function جدیدی ایجاد نشده و PostgreSQL تغییر ساختاری ندارد.

نکته:
این «نمایش تلفیقی معتبر با provenance» است؛ متن ذخیره‌شده سند اصلی بازنویسی نمی‌شود.
