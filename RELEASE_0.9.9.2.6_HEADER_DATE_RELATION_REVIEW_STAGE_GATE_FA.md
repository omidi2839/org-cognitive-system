# SINA 0.9.9.2.6 — Header Date + Relation Review + Stage Gate

اصلاحات این نسخه:

- تشخیص تاریخ‌های سربرگ Word برای حالت‌های رایج `YYYY/MM/DD` و نمایش RTL معکوس `DD/MM/YYYY` و فاصله‌های اطراف `/` یا `-` تقویت شد.
- متن‌های Header/Footer از runهای متنی و field-resultهای Word با پوشش بیشتر خوانده می‌شوند.
- تاریخ سند، تاریخ جلسه، تاریخ ابلاغ، شماره جلسه و شماره سند در جدول بازبینی دسته‌جمعی به‌صورت فیلد قابل اصلاح نمایش داده می‌شوند.
- روابطی که هوش مصنوعی شناسایی می‌کند دیگر فقط یک شمارنده نیستند:
  - نوع رابطه
  - سند مقصد
  - شاهد/توضیح
  - تطبیق پیشنهادی با سند موجود
  - تأیید یا رد رابطه
  در مرحله بازبینی قابل مشاهده و اصلاح هستند.
- فقط روابطی که کاربر صریحاً «تأیید رابطه» را فعال کند در `documentRelations` ثبت می‌شوند و بعداً در صفحه سند قابل مشاهده‌اند.
- گردش چهارمرحله‌ای واقعی شد:
  1. انتخاب فایل‌ها
  2. پردازش و شناسایی
  3. بازبینی و اصلاح توسط کاربر
  4. ثبت نهایی
  پایان پردازش دیگر به‌طور خودکار وارد مرحله ۳ یا ۴ نمی‌شود.
- مرحله ۴ فقط پس از موفقیت واقعی API ثبت نهایی سبز می‌شود.
