import { createRepository } from '../src/infrastructure/repositoryFactory.js';
const ORG='ORG:SYN-001';
const send=(res,status,data)=>{res.statusCode=status;res.setHeader('content-type','application/json; charset=utf-8');res.end(JSON.stringify(data))};
const bodyOf=req=>{if(!req.body)return{};if(typeof req.body==='object')return req.body;try{return JSON.parse(req.body)}catch{return{}}};
const actorOf=req=>{const h=req.headers||{},role=String(h['x-role']||'admin').toLowerCase();return{organizationId:String(h['x-org-id']||ORG),role,personRef:String(h['x-person-id']||h['x-user-id']||'current-user'),canEdit:String(h['x-document-edit-permission']||'').toLowerCase()==='true'||['admin','administrator','document_editor','knowledge_admin'].includes(role)}};
const editable=['title','documentType','documentNumber','issuer','versionLabel','issuedAt','promulgationDate','meetingType','meetingNumber','meetingDate','validUntil','validityStatus','classification','organizationalLevel','scopeType','organizationalUnitRef','organizationalUnitName','subjectCategory','subjectArea'];
export default async function handler(req,res){try{const actor=actorOf(req),repo=createRepository(),db=await repo.all(),u=new URL(req.url,'https://local'),id=String(u.searchParams.get('documentId')||'');
if(req.method==='GET'){if(!id)return send(res,200,{permissions:{documentEdit:actor.canEdit,role:actor.role}});const d=(db.documents||[]).find(x=>x.id===id&&x.organizationId===actor.organizationId);if(!d)return send(res,404,{message:'سند پیدا نشد'});return send(res,200,{document:d,permissions:{documentEdit:actor.canEdit,role:actor.role}})}
if(req.method!=='PATCH')return send(res,405,{message:'Method not allowed'});if(!actor.canEdit)return send(res,403,{message:'شما مجوز ویرایش سند را ندارید',code:'DOCUMENT_EDIT_FORBIDDEN'});if(!id)return send(res,400,{message:'شناسه سند الزامی است'});
const input=bodyOf(req),patch={};for(const k of editable)if(Object.prototype.hasOwnProperty.call(input,k))patch[k]=input[k]===undefined?null:input[k];if(!Object.keys(patch).length)return send(res,400,{message:'هیچ فیلد قابل ویرایشی ارسال نشده است'});
const now=new Date().toISOString();let updated=null;await repo.mutate(s=>{s.documents??=[];const d=s.documents.find(x=>x.id===id&&x.organizationId===actor.organizationId);if(!d)return;const before={};for(const k of Object.keys(patch))before[k]=d[k]??null;Object.assign(d,patch,{editedAt:now,editedBy:actor.personRef});s.documentEditAudit??=[];s.documentEditAudit.push({id:`DEA:${Date.now()}:${Math.random().toString(36).slice(2,8)}`,organizationId:actor.organizationId,documentRef:id,editedAt:now,editedBy:actor.personRef,before,after:patch});updated={...d}});if(!updated)return send(res,404,{message:'سند پیدا نشد'});return send(res,200,{document:updated,auditRecorded:true});
}catch(e){console.error('DOCUMENT_GOVERNANCE_ERROR',e);return send(res,500,{message:e?.message||'خطا در مدیریت سند',code:'DOCUMENT_GOVERNANCE_ERROR'})}}