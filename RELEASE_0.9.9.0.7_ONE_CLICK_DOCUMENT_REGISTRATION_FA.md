# Hotfix 0.9.9.0.7 — ثبت قطعی سند با یک کلیک

## مسئله
در برخی ثبت‌ها، کلیک اول فرایند آماده‌سازی/ارسال فایل را انجام می‌داد اما مسیر legacy فرم برای تکمیل ثبت به تلاش دوم وابسته می‌شد. همچنین فایل در لایه پایه ابتدا Base64 می‌شد و سپس لایه transport دوباره آن را آماده می‌کرد که زمان ثبت را افزایش می‌داد.

## اصلاح
- مسیر ثبت فرم `#k76form` اکنون یک orchestration واحد و capture-safe دارد.
- کلیک ثبت، کل چرخه را در همان تلاش انجام می‌دهد: validation → transport → upload → document commit → relation commit → success.
- handler قدیمی فرم پس از تصاحب ثبت توسط مسیر جدید اجرا نمی‌شود؛ بنابراین آماده‌سازی فایل دوبار انجام نمی‌شود.
- `clientRequestId` در تمام retryها ثابت می‌ماند تا تلاش شبکه‌ای مجدد سند تکراری نسازد.
- خطاهای موقت 408/425/429/500/502/503/504 در همان کلیک یک بار retry می‌شوند.
- Blob-not-found برای مجموعه فایل‌های کوچک، در همان کلیک به Base64 recovery تبدیل می‌شود.
- روابط حقوقی نیز پیش از نمایش موفقیت به‌صورت await شده Commit می‌شوند؛ پیام موفقیت فقط پس از پایان چرخه کامل نمایش داده می‌شود.
- دکمه تا پایان عملیات غیرفعال می‌ماند و کلیک موازی ایجاد نمی‌شود.

## Baseline
تمام قابلیت‌های 0.9.9.0.6 و پیش از آن، از جمله فارسی‌سازی اعداد، لینک نتایج جستجو، پروفایل/خروج، چند ارتباط حقوقی، normalization محل حقوقی، مرزبندی عنوان ماده بعدی و الحاق ماده جدید بعد از آخرین ماده حفظ شده‌اند.

هیچ API جدیدی اضافه نشده است.
