# Feature 0.9.6.0 — Inline Amendment View

## تغییرات
- دکمه‌های بالای پنجره نمایش سند کوچک‌تر و منظم‌تر شدند.
- برای سند اصلی، ارتباطات ورودی از نوع اصلاح/الحاق/جایگزینی/تبیین خوانده می‌شوند.
- `changeItems` بر اساس شماره ماده در متن سند اصلی جای‌گذاری می‌شوند.
- اصلاح در انتهای همان ماده و پیش از ماده بعدی با رنگ متفاوت نمایش داده می‌شود.
- منبع اصلاح قابل کلیک است و سند اصلاحی را باز می‌کند.
- این نمایش «تلفیقی نمایشی» است؛ متن اصلی ذخیره‌شده را بازنویسی نمی‌کند و provenance حفظ می‌شود.
- اعداد داخل متن در `<bdi dir="ltr">` ایزوله می‌شوند تا مواردی مثل ۱۷ به‌صورت ۷۱ نمایش داده نشوند.
- هیچ API Function جدیدی اضافه نشده و PostgreSQL تغییر ساختاری ندارد.

## نکته
برای نمایش متن دقیق الحاق/اصلاح، مقدار `changeItems.description` باید هنگام ثبت ارتباط شامل متن یا شرح دقیق تغییر باشد.
