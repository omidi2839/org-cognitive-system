# 0.9.9.0.31

## کارت‌های اسناد عمومی و بالادستی
- خود کارت‌ها دیگر هیچ قابلیت Navigation ندارند.
- کارت قدیمی یک‌بار clone می‌شود تا listenerهای click قدیمی نیز حذف شوند.
- `data-capability` و کلاس clickable از این دو کارت برداشته می‌شود.
- تنها مسیر ورود، متن نارنجی «ثبت سند عمومی/بالادستی» است.
- فونت CTA کمی بزرگ‌تر و واضح‌تر شد.

## بازگشت به دانش و اسناد سازمان
- رنگ قرمز تند حذف شد.
- CTA بازگشت به طیف آبی-خاکستری هماهنگ با رابط سینا تغییر کرد؛ همچنان واضح و قابل تشخیص است.

## رفع 504 و ثبت طولانی نظر خبرگانی
علت اصلی دو بخش داشت:
1. شمارنده‌ها روی هر Mutation کل صفحه سه API را دوباره صدا می‌زدند؛ تغییر خود شمارنده نیز Mutation جدید ایجاد می‌کرد و می‌توانست چرخه درخواست‌های مکرر documents/collaborative-analysis/macro-knowledge بسازد.
2. هر ثبت نظر کل runtime state را با gzip level 9 دوباره فشرده می‌کرد.

اصلاح:
- MutationObserver دیگر هیچ درخواست شبکه‌ای اجرا نمی‌کند و فقط شمارنده cache شده را روی DOM اعمال می‌کند.
- شمارنده‌ها حداکثر هر ۳۰ ثانیه refresh شبکه‌ای می‌شوند و درخواست همزمان dedupe می‌شود.
- Retry خودکار PATCH حذف شد تا دو write همزمان روی row lock ایجاد نشود.
- gzip runtime state از level 9 به level 3 تغییر کرد.
- clone اضافی state در cache repository حذف شد.

این تغییرات باید بار همزمان API را به‌طور محسوسی کاهش دهد و مشکل تکرارشونده 504 هنگام ثبت نقد/نظر را برطرف کند.
