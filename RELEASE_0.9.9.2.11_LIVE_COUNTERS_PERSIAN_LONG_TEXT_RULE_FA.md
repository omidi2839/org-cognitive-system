# SINA 0.9.9.2.11

## 1) شمارنده‌های زنده و قانون ثابت کارت‌ها
- سندهای soft-deleted دیگر در API `/knowledge/documents` و هیچ summary فعال شمارش نمی‌شوند.
- پس از حذف، ثبت تکی و ثبت دسته‌جمعی، event سراسری `sina:data-changed` صادر می‌شود.
- شمارنده کارت‌ها فوراً از API اصلی با `no-store` بازخوانی می‌شود؛ cache سی‌ثانیه‌ای دیگر باعث عدد قدیمی نمی‌شود.
- قرارداد ثابت طراحی اضافه شد: هر کارت capability می‌تواند از سیستم شمارنده مشترک استفاده کند و در آینده فقط metric جدید به registry اضافه می‌شود.
- کارت «دانش کلان» اکنون شمارنده ترکیبی دارد: «X مفهوم · Y دانش».
- کارت‌های اسناد بالادستی، اسناد عمومی، بانک اسناد و تحلیل اسناد نیز از همان موتور شمارنده استفاده می‌کنند.

## 2) قانون ثابت متن‌های بلند فارسی
- متن‌های بلند فارسی در کارت‌های دانش، مفاهیم/منابع، شاهدها و نقدها `justify` می‌شوند.
- کارت‌های متنی بلند ارتفاع کنترل‌شده دارند و اسکرول داخلی می‌گیرند تا طول متن باعث کشیده‌شدن کل صفحه نشود.
- متن کامل سند در محیط پژوهش نیز justify می‌شود و اسکرول فعلی خود را حفظ می‌کند.

## 3) Cache-safe
- `knowledge-governance-099211.js/css`
- `document-bank-099211.js`
