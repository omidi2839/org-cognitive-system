# Hotfix 0.9.9.0.6

## یکپارچه‌سازی اعداد فارسی در رابط کاربری
- اعداد انگلیسی موجود در متن قابل مشاهده رابط، منوها، باکس‌ها، labelها، optionها، placeholderها و خروجی‌های جستجو به رقم فارسی تبدیل می‌شوند.
- در فیلدهای متنی و جستجو نیز اگر کاربر عدد انگلیسی تایپ کند، نمایش آن به فارسی تبدیل می‌شود.
- فیلدهای ماشینی حساس مانند password، file، email، url و input type=number دست‌کاری نمی‌شوند.

## لینک اسناد در نتایج میزکار
- عنوان سند و شواهد متنی در `commandResult` مستقیماً به `window.__ORG_OPEN_DOCUMENT__` متصل شدند.
- کلیک روی سند جستجو شده، همان modal بانک اسناد را باز می‌کند و شناسه سند فعال را همزمان در همه لایه‌های فعلی تنظیم می‌کند.

## منوی پروفایل و خروج
- آیکن پروفایل بالای سامانه به منوی واقعی تبدیل شد.
- گزینه «خروج از سامانه» زیر همان آیکن قرار گرفت.
- خروج با `DELETE /api/v1/auth/session` نشست امن را پاک می‌کند و صفحه ورود مجدداً نمایش داده می‌شود.

هیچ API جدیدی اضافه نشده است؛ تعداد فایل‌های top-level پوشه api همچنان 8 است.
