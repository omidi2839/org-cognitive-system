# 0.9.8.3 — محیط پژوهشی تحلیل خبرگان

این نسخه Deployment-Safe است و Function جدیدی در `/api` ایجاد نمی‌کند.

## اصلاحات
- فرم ویرایش سند به فرم شناسنامه‌ای مشابه ثبت سند تبدیل شد و فیلدهای تاریخ با همان تقویم جلالی سامانه نمایش داده می‌شوند.
- موضوع کلان، زیرموضوع، جلسه، شماره سند، اعتبار، طبقه‌بندی و دامنه سازمانی قابل ویرایش هستند.
- استخراج اصلاحیه برای `متن م ص و به`، `متن مصوبه`، `ماده واحده` و عنوان‌های «در خصوص...» مقاوم‌تر شد و متن هم‌خط بعد از ماده واحده از دست نمی‌رود.
- محیط تحلیل به میز پژوهشی تبدیل شد: متن کامل سند بالادستی در سمت اصلی نمایش داده می‌شود.
- خبره بخشی از متن را انتخاب می‌کند؛ همان انتخاب به مفهوم/شاهد یادداشت متصل می‌شود.
- نام خبره از کاربر واردشده سامانه گرفته می‌شود و نام کارگروه ثابت «گروه خبرگان تحلیل اسناد بالادستی» است.
- یادداشت می‌تواند متنی یا صوتی باشد. ضبط مستقیم میکروفن و بارگذاری فایل صوتی برای تست فعال است؛ برای جلوگیری از بزرگ‌شدن JSONB، در این نسخه آزمایشی سقف صوت ۱.۵MB است.
- تمام منطق 0.9.7.5 برای ماده/تبصره/بند و append/replace حفظ شده است.
