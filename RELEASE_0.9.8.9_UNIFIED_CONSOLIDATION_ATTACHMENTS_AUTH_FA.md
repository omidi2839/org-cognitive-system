# نسخه 0.9.8.9 — بسته یکپارچه پایداری و تجربه سند

این نسخه بر پایه 0.9.8.8.5 ساخته شده و همه اصلاحات این مرحله را یکجا دارد.

- رفع اشکال جهت رابطه قدیمی «اصلاحیه سند قبلی» و ترمیم خودکار رابطه‌های Legacy؛ در نتیجه اصلاحیه/الحاقیه جدید مانند مصوبه 291 در نمای مؤثر سند مادر در محل ماده هدف اعمال می‌شود.
- رفع سفید شدن فضای کاری پس از رفتن به «میزکار شناختی من» و بازگشت دوباره به گزینه‌های منوی سمت چپ.
- حفظ انتخاب چندتایی پیوست برای ثبت و ویرایش سند و بازطراحی بصری ناحیه‌های فایل.
- نمایش تعداد و نام پیوست‌ها در بالای پنجره متن سند؛ هر پیوست با لینک کوتاه‌عمر Private Blob قابل باز کردن است.
- افزودن ورودی مدیریتی با نام کاربری/رمز عبور، Cookie امن HttpOnly و بارگذاری رابط اصلی فقط پس از احراز هویت.
- محافظت از APIهای تخصصی و مسیر wildcard سامانه با نشست مدیریتی؛ auth-proxy درخواست‌های عمومی API را قبل از رسیدن به api/index.js کنترل می‌کند.

## حساب موقت مدیر کل
نام کاربری: `admin`
رمز عبور: `Cognitive@1405!`

برای محیط عملیاتی می‌توان بدون تغییر کد، متغیرهای `ADMIN_USERNAME`، `ADMIN_PASSWORD` و ترجیحاً `APP_AUTH_SECRET` را در Vercel تنظیم کرد. حساب پیش‌فرض فقط برای مرحله فعلی توسعه است.

## نشانگر Build
`window.__PLATFORM_UNIFIED_BUILD__` باید `0.9.8.9` باشد. همچنین نشانگرهای Document Meeting و Document Bank نیز `0.9.8.9` هستند.
