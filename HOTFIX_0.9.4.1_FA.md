# Hotfix 0.9.4.1 — Relation Preview Timing + Direction Fix

## علت نمایش ندادن ارتباط در Popup
در 0.9.4.0 پس از کلیک روی سند، کد نمایش ارتباط فقط 180ms صبر می‌کرد.
اما Popup ابتدا در حالت «در حال دریافت متن سند…» ساخته می‌شود و `.k91modalbody` فقط بعد از پایان API متن سند ایجاد می‌شود.
اگر API بیشتر از 180ms طول می‌کشید، تابع نمایش Relation اجرا می‌شد، body را پیدا نمی‌کرد و دیگر تکرار نمی‌شد.

## اصلاح
این Hotfix حداکثر 5 ثانیه، هر 80ms منتظر آماده‌شدن `.k91modalbody` می‌ماند و فقط یک بار Relation را نمایش می‌دهد؛ حلقه MutationObserver شبکه‌ای ایجاد نمی‌شود.

## اصلاح دوم: جهت رابطه
در UI افزودن سند، گزینه «اصلاحیه سند قبلی» قبلاً مقدار `amended_by` داشت.
از نظر معنایی، سند جدید باید `amends` سند قبلی باشد.
Hotfix مقدار این گزینه را به `amends` تبدیل می‌کند تا:
- سند پایه: «اصلاح‌شده توسط سند جدید»
- سند جدید: «اصلاح‌کننده سند پایه»
درست نمایش داده شود.

## نصب
1. فایل `public/document-relations-preview-fix-0941.js` را اضافه کنید.
2. در `public/index.html` بلافاصله بعد از `document-amendments-093.js?v=0.9.4.0` این خط را اضافه کنید:
   `<script src="./document-relations-preview-fix-0941.js?v=0.9.4.1"></script>`
3. Deploy و Ctrl+F5.

## نکته روابط قبلی
روابطی که با 0.9.4.0 و گزینه «اصلاحیه سند قبلی» ثبت شده‌اند ممکن است جهت معکوس داشته باشند. این Hotfix روابط جدید را درست ثبت می‌کند. برای روابط قبلی در نسخه بعدی می‌توان Migration/Repair خودکار اضافه کرد.
